# Pre-cleanup recovery checkpoint

This checkpoint preserves the user-edited manual-routing canvas and its last validation summary before obsolete V3 build candidates were removed from the working tree.

It is historical reference material, not an input to the four-channel design. The authoritative macro is declared by `v3/CURRENT.json`.

